services:
  kjol:
    build: .
    container_name: kjol
    restart: unless-stopped
    ports:
      - "8357:3000"
    volumes:
      - kjol-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/kjol.json

volumes:
  kjol-data:
